(ns ch1-statistics.core
  (:require [ch1-statistics.data :as dt]
            [incanter.core :as ic])
  (:gen-class))






(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
